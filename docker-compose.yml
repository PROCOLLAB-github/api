version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8000:8000"
    image: app
    restart: always
    env_file:
      - .env
    environment:
      HOST: 0.0.0.0